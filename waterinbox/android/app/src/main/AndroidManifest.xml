<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Waterinbox">
        <!--
            Box space is pinned to the device's NATURAL orientation and never follows
            the system display rotation:
              * screenOrientation="nosensor" -> window keeps the device natural
                orientation, ignoring the sensors and the user's rotation setting.
                Declared here (not set at runtime) so WindowManager knows the request
                before launch and can pre-rotate the display (no visible flip).
              * configChanges -> do not recreate the activity on configuration changes;
                recreation would reset the sensor fusion quaternion (SensorProbe),
                re-roll the random render group (BoxSpace), and flash the GL surface.
        -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="nosensor"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden"
            android:theme="@style/Theme.Waterinbox">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>